# Copy this file to .env.local and adjust if needed.

# Base URL of the ASP.NET backend. The Next.js dev server proxies /api/* to
# this address, so the frontend can call same-origin /api/Projects etc. and
# you don't need to enable CORS on the backend.
BACKEND_URL=http://localhost:5094
